const React = window.React; 
const Redux = window.Redux;
const {
    Provider, 
    useSelector, 
    useDispatch} = window.ReactRedux; 
const { 
    BrowserRouter,
    Switch,
    Route,
    Link,
    Redirect,
    withRouter
} = window.ReactRouterDOM; 
const ReduxThunk = window.ReduxThunk.default; 
const axios = window.axios; 
 
